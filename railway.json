{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "sh -c 'cd dashboard-medico && pnpm install && pnpm build && pnpm start'",
    "restartPolicyMaxRetries": 5
  }
}
